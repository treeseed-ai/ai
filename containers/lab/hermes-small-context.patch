--- a/agent/model_metadata.py
+++ b/agent/model_metadata.py
@@ -182,7 +182,10 @@ DEFAULT_FALLBACK_CONTEXT = CONTEXT_PROBE_TIERS[0]
 # Minimum context length required to run Hermes Agent.  Models with fewer
 # tokens cannot maintain enough working memory for tool-calling workflows.
 # Sessions, model switches, and cron jobs should reject models below this.
-MINIMUM_CONTEXT_LENGTH = 64_000
+# TreeAI runs a deliberately bounded toolset and supplies an explicit output
+# budget for its qualified 16K local profile. This scoped image patch does not
+# change upstream Hermes installations or claim that the provider serves 64K.
+MINIMUM_CONTEXT_LENGTH = 16_384
