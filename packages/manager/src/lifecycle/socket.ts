import{connect}from'node:net';import{paths}from'../core/paths.js';
export const supervisorOperations=['auth.rotate','update.check','update.plan','update.apply','update.cancel-before-install','update.channel.set','update.pause','update.resume','config.adopt','config.validate','component.enable','component.disable','recovery.retry','recovery.restore-generation','mode.set','reconcile']as const;
export type SupervisorOperation=typeof supervisorOperations[number];
export interface SupervisorRequest{operation:SupervisorOperation;parameters?:Record<string,unknown>;idempotencyKey:string}
export function callSupervisor(request:SupervisorRequest):Promise<unknown>{if(!supervisorOperations.includes(request.operation))throw new Error('Operation is not allowlisted.');return new Promise((resolve,reject)=>{const client=connect(paths.socket);let data='';client.setEncoding('utf8');client.on('connect',()=>client.end(`${JSON.stringify(request)}\n`));client.on('data',chunk=>{data+=chunk;});client.on('error',reject);client.on('close',()=>{try{const value=JSON.parse(data)as{ok:boolean;result?:unknown;error?:{message:string}};value.ok?resolve(value.result):reject(new Error(value.error?.message??'Supervisor rejected the operation.'));}catch(error){reject(error);}});});}
