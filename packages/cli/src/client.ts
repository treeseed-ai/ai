import{execFileSync}from'node:child_process';import{readFileSync}from'node:fs';import{hostname}from'node:os';import{configPath,keyPath,type ClientConfig}from'./contracts.js';
export function config():ClientConfig{try{return JSON.parse(readFileSync(configPath,'utf8'))as ClientConfig;}catch{const host=process.env.AI_FACTORY_HOST??hostname();return{schemaVersion:'treeai.config/v1',version:'0.6.1',imageSource:'local-build',ca:process.env.AI_FACTORY_CA_CERT??'/etc/ssl/certs/treeseed-ai-factory-development-ca.pem',endpoints:{manager:`https://${host}:4790`,inference:`https://${host}:4770`,openai:`https://${host}:4771`,training:`https://${host}:4780`,lab:`https://${host}:4793`},installedProducts:[]};}}
export function key(){const environment=process.env.TREEAI_OPERATOR_KEY_VALUE;if(environment)return environment;return readFileSync(keyPath,'utf8').trim();}
export function request(surface:string,path:string,init:{method?:string;body?:string}={}){const settings=config(),endpoint=settings.endpoints[surface];if(!endpoint)throw new Error(`No ${surface} endpoint is configured.`);const args=['--silent','--show-error','--fail-with-body','--cacert',settings.ca,'-H',`Authorization: Bearer ${key()}`,'-H','content-type: application/json','-X',init.method??'GET'];if(init.body)args.push('--data',init.body);args.push(`${endpoint}${path}`);const text=execFileSync('curl',args,{encoding:'utf8'});try{return JSON.parse(text)as unknown;}catch{return text;}}
