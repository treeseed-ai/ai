import{Buffer}from'node:buffer';import{describe,expect,it,vi}from'vitest';import{createLibraryBridge}from'../../packages/lab/src/library-bridge.ts';
describe('Open WebUI library bridge streaming',()=>{it('preserves bodies without forwarding hop-by-hop framing headers',async()=>{const requestFetch=vi.fn(async(_input:string|URL|Request,init?:RequestInit)=>{expect(new Headers(init?.headers).get('accept-encoding')).toBe('identity');return new Response('<!doctype html><main>TreeAI</main>',{headers:{'content-type':'text/html','transfer-encoding':'chunked','connection':'keep-alive','trailer':'x-checksum'}});}),app=createLibraryBridge({fetch:requestFetch as typeof fetch,trainingKey:'key'}),response=await app.request('/');expect(response.status).toBe(200);expect(await response.text()).toBe('<!doctype html><main>TreeAI</main>');expect(response.headers.get('transfer-encoding')).toBeNull();expect(response.headers.get('connection')).toBeNull();expect(response.headers.get('trailer')).toBeNull();expect(requestFetch).toHaveBeenCalledTimes(1);});});
