import{describe,expect,it}from'vitest';import{documentProcessingFailure,transitionFailure,transitionObservation}from'../../packages/lab/src/library-cycles.js';
describe('library cycle transition errors',()=>{it('retains bounded manager errors and reasons',()=>{expect(transitionFailure('sleep',{state:'failed',error:'marker unhealthy',result:{reason:'gpu worker unavailable'}})).toBe('Factory sleep transition failed: marker unhealthy; gpu worker unavailable');});it('redacts credentials and host paths',()=>{const value=transitionFailure('awake',{state:'postponed',error:'token=secret-value /home/adrian/private'});expect(value).toContain('[REDACTED]');expect(value).not.toContain('secret-value');expect(value).not.toContain('/home/adrian');});it('detects terminal jobs only for current unready document revisions',()=>{const documents=[{state:'pending_processing',currentRevision:{id:'revision-2'}}],old={id:'old',type:'library.document.marker',state:'failed',request:{revisionId:'revision-1'},error:{message:'old failure'}},current={id:'current',type:'library.document.marker',state:'failed',request:{revisionId:'revision-2'},error:{message:'marker failed'}};expect(documentProcessingFailure(documents,[old])).toBeUndefined();expect(documentProcessingFailure(documents,[old,current])).toBe('Document processing failed for job current: marker failed');expect(documentProcessingFailure([{...documents[0],state:'ready'}],[current])).toBeUndefined();});});
describe('factory transition observation',()=>{it('accepts the authoritative target even if the work receipt was interrupted',()=>{expect(transitionObservation('sleep','running','sleep',20_000)).toBe('succeeded');});it('fails a stale running receipt after recovery to another stable mode',()=>{expect(transitionObservation('sleep','running','awake',20_000)).toBe('interrupted');});it('allows queue and supervisor handoff time',()=>{expect(transitionObservation('sleep','queued','awake',20_000)).toBe('pending');expect(transitionObservation('sleep','running','awake',5_000)).toBe('pending');expect(transitionObservation('sleep','running','transitioning_sleep',20_000)).toBe('pending');});});
