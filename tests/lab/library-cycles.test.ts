import{describe,expect,it}from'vitest';import{documentProcessingFailure,transitionFailure}from'../../packages/lab/src/library-cycles.js';
describe('library cycle transition errors',()=>{it('retains bounded manager errors and reasons',()=>{expect(transitionFailure('sleep',{state:'failed',error:'marker unhealthy',result:{reason:'gpu worker unavailable'}})).toBe('Factory sleep transition failed: marker unhealthy; gpu worker unavailable');});it('redacts credentials and host paths',()=>{const value=transitionFailure('awake',{state:'postponed',error:'token=secret-value /home/adrian/private'});expect(value).toContain('[REDACTED]');expect(value).not.toContain('secret-value');expect(value).not.toContain('/home/adrian');});it('detects terminal jobs only for current document revisions',()=>{const documents=[{currentRevision:{id:'revision-2'}}],old={id:'old',type:'library.document.marker',state:'failed',request:{revisionId:'revision-1'},error:{message:'old failure'}},current={id:'current',type:'library.document.marker',state:'failed',request:{revisionId:'revision-2'},error:{message:'marker failed'}};expect(documentProcessingFailure(documents,[old])).toBeUndefined();expect(documentProcessingFailure(documents,[old,current])).toBe('Document processing failed for job current: marker failed');});});
