import{describe,expect,it}from'vitest';import{hashApiKey,MemoryJobRepository}from'../../packages/common/src/index.ts';import{createTrainingControlApp,trainingRoutes}from'../../packages/training-api/src/app.ts';
const record={id:'test',hash:hashApiKey('secret','salt'),scopes:['*'],revoked:false};const resolveKey=async(id:string)=>id==='test'?record:null;const headers={authorization:'Bearer ak_test_secret','content-type':'application/json'};
describe('training product',()=>{it('declares every route in OpenAPI 3.1.1',async()=>{const app=createTrainingControlApp({jobs:new MemoryJobRepository(),resolveKey});const document=await(await app.request('/openapi.json')).json()as any;expect(document.openapi).toBe('3.1.1');for(const route of trainingRoutes)expect(document.paths[route.path.replace(/:([^/]+)/g,'{$1}')][route.method.toLowerCase()]).toBeDefined();});it('creates PDF and QLoRA jobs without accepting host commands',async()=>{const app=createTrainingControlApp({jobs:new MemoryJobRepository(),resolveKey});const pdf=await app.request('/v1/documents',{method:'POST',headers,body:JSON.stringify({idempotencyKey:'pdf-1',input:{sourceUri:'s3://input/a.pdf',command:'rm'}})});expect(pdf.status).toBe(202);expect(await pdf.json()).toMatchObject({type:'document.process'});const train=await app.request('/v1/training-runs',{method:'POST',headers,body:JSON.stringify({idempotencyKey:'train-1',input:{config:{adapter:'qlora'}}})});expect(train.status).toBe(202);expect(await train.json()).toMatchObject({type:'training.qlora'});});});
