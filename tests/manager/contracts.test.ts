import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';import{validateCatalog}from'../../packages/manager/src/core/catalog.js';import{createManagerApp}from'../../packages/manager/src/http/api.js';
describe('manager contracts',()=>{it('validates the release catalog and distinct signing keys',()=>{const catalog=validateCatalog(JSON.parse(readFileSync('release/catalog.json','utf8')));expect(catalog.generation).toBe(6001000);expect(readFileSync('release/apt/RELEASE_KEY_FINGERPRINT','utf8').trim()).not.toBe(readFileSync('release/apt-development/RELEASE_KEY_FINGERPRINT','utf8').trim());});it('publishes OpenAPI 3.1.1 without exposing apply',async()=>{const response=await createManagerApp().request('/openapi.json'),document=await response.json()as{openapi:string;paths:Record<string,unknown>};expect(document.openapi).toBe('3.1.1');expect(document.paths['/v1/updates/check']).toBeTruthy();expect(document.paths['/v1/updates/plan']).toBeTruthy();expect(document.paths['/v1/updates/apply']).toBeUndefined();});it('keeps update execution on a fixed supervisor socket',()=>{const socket=readFileSync('packages/manager/src/lifecycle/socket.ts','utf8'),supervisor=readFileSync('packages/manager/src/lifecycle/supervisor.ts','utf8');expect(socket).toMatch(/["']update\.apply["']/u);expect(supervisor).toMatch(/case\s+["']update\.apply["']/u);expect(supervisor).not.toContain('exec(');});});
