import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';import{configurationDigest,finalizeConfiguration,validatePlatformConfiguration}from'../../packages/common/src/platform/index.js';
describe('platform desired state',()=>{it('finalizes and validates the generic factory seed',()=>{const value=JSON.parse(readFileSync('config/platform.default.json','utf8')),final=finalizeConfiguration(value);expect(final.provenance.configurationDigest).toMatch(/^[a-f0-9]{64}$/u);expect(configurationDigest(final)).toBe(final.provenance.configurationDigest);expect(validatePlatformConfiguration(final).updates.channel).toBe('stable');});it('requires a 60 second continuous development cadence',()=>{const value=finalizeConfiguration(JSON.parse(readFileSync('config/platform.default.json','utf8')));value.updates.channel='development';value.updates.policy='continuous';value.updates.pollSeconds=61;expect(()=>validatePlatformConfiguration(value)).toThrow(/60 seconds/u);});it('rejects credential-shaped external secret values',()=>{const value=finalizeConfiguration(JSON.parse(readFileSync('config/platform.default.json','utf8')));value.secrets.database={provider:'file',reference:'password=exposed'};expect(()=>validatePlatformConfiguration(value)).toThrow(/provider reference/u);});});
