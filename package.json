{
  "name": "@ai-platform/workspace",
  "version": "0.8.0",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/treeseed-ai/ai.git"
  },
  "homepage": "https://treeseed.dev",
  "bugs": {
    "url": "https://github.com/treeseed-ai/ai/issues"
  },
  "type": "module",
  "packageManager": "pnpm@10.15.1",
  "engines": { "node": ">=22" },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm --filter @ai-platform/common build && pnpm --filter @ai-platform/manager build && pnpm --filter @ai-platform/cli build && pnpm --filter @ai-platform/host-runtime build && pnpm --filter @ai-platform/inference-api build && pnpm --filter @ai-platform/inference-manager build && pnpm --filter @ai-platform/training-api build && pnpm --filter @ai-platform/training-manager build && pnpm --filter @ai-platform/lab build",
    "test": "vitest run",
    "check:file-architecture": "node --import tsx scripts/support/check-source-shape.ts",
    "check:release": "node --import tsx scripts/release/validate.ts",
    "build:deb": "pnpm build && node --import tsx scripts/package-deb.ts all",
    "verify": "pnpm check:release && pnpm check:file-architecture && pnpm build && pnpm test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.879.0",
    "@hono/node-server": "^1.19.9",
    "hono": "^4.11.4",
    "pg": "^8.16.3",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/pg": "^8.15.5",
    "esbuild": "^0.28.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
