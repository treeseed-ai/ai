# Development archive key

This directory contains only the public TreeAI development APT key and its primary-key fingerprint. The encrypted private signing key and passphrase are stored as protected secrets in the GitHub `staging` environment. Development artifacts are never signed with the stable release key.
