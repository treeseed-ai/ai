#!/bin/sh
set -eu
if [ "${1:-}" = remove ]; then systemctl disable --now treeseed-ai-manager-stable.timer treeseed-ai-manager-development.timer treeseed-ai-manager-api.service treeseed-ai-manager-supervisor.service >/dev/null 2>&1 || true; fi
exit 0
